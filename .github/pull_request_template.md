<!--
The TITLE of this pull request is the commit that lands on main, so it has to read as one:

    type(scope): Capitalised prose, no full stop
    feat(proxy)!: Decide on the same request you send

Types: feat fix perf refactor revert build docs test ci chore
`!` if this breaks an action input, a CLI flag, an environment variable, an exit code, or the
policy format. See CONTRIBUTING.md.

Does this need to ship? Edit VERSION. Leaving it alone is fine — the change waits for the next
release. The Conventions check will tell you the smallest number that covers everything unreleased.
-->

## What and why
